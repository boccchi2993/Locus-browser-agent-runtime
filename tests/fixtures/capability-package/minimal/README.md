# minimal capability package fixture (TEST ONLY)

Minimal Capability Project used by `tests/capability-package.test.cjs`.
Not a production catalog entry. The wheel under
`plugins/locus-test-plugin/artifacts/` is regenerated deterministically by
`../tools/build-wheel.py` (Python stdlib only, fixed timestamps).
